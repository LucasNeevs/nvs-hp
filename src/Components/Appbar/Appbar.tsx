import React, { ReactElement } from 'react';
import * as S from './Styles';

export default (): ReactElement => (
  <S.Nav>
    <S.Title
      variant="h5"
      noWrap
    >
      HP
    </S.Title>
  </S.Nav>
);
