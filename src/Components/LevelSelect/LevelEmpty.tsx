import React from 'react';

export default (): JSX.Element => (
  <div style={{
    width: '100%',
    height: 250,
    backgroundColor: '#000',
    color: '#fff',
  }}>
    <h1>Select a level to start the game!</h1>
  </div>
);
